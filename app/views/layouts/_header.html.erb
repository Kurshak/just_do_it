<header class="navbar navbar-fixed-top navbar-inverse "style="background-color: #FF3333;">
  <div class="container">
    <%= link_to "Just do it ", root_path, id: "logo" %>
        <ul >
          <% if(user_signed_in?)%>
              <li ><%= link_to "Log out", destroy_user_session_path, method: "delete" %></li>
            <% else %>
               <li > <%= link_to "Sign up now!", new_user_registration_path %> </li>
               <li > <%= link_to "Log in", new_user_session_path %> </li>
          <% end %>
        </ul>
  </div>
</header>